/* General utilities
   (c) Reuben Thomas 2000
*/


#ifndef MITE_UTIL
#define MITE_UTIL


#include <stdint.h>

#include "endian.h"
#include "except.h"
#include "buffer.h"
#include "list.h"
#include "hash.h"
#include "flen.h"


#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) > (b) ? (a) : (b))

#endif
