/* General utilities
   (c) Reuben Thomas 2000
*/


#ifndef MITE_UTIL
#define MITE_UTIL

#include "config.h"

#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) > (b) ? (a) : (b))

#endif
